# Teller App Requirements
#
# This file lists the dependencies needed to run the Teller banking system.
# It is used by the installation scripts to verify and install required dependencies.

##############################
# System Dependencies
# These are required system packages
##############################

# Essential system packages
docker
docker-compose
git
curl

##############################
# Python Dependencies
# These will be installed by pip
##############################
requests>=2.28.0
python-dotenv>=0.20.0
pyyaml>=6.0
frappe-bench

# Optional Tools (system packages, not for pip)
# dos2unix       # For line ending conversion (may be needed for scripts) 