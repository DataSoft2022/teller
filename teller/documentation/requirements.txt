# Teller App Requirements
#
# This file lists the dependencies needed to run the Teller banking system.
# It is used by the installation scripts to verify and install required dependencies.

# System Dependencies
# These are required system packages
docker
docker-compose
git
curl
wget
dos2unix

# Python Dependencies
# These are required Python packages
requests>=2.28.0
python-dotenv>=0.20.0
pyyaml>=6.0
frappe-bench

# Optional Tools
# dos2unix>=7.4.0       # For line ending conversion (uncomment if needed) 